// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT
import GoogleRpc

/// BackupConfigInfo has information about how the resource is configured
/// for Backup and about the most recent backup to this vault.
public struct BackupConfigInfo: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Output only. The status of the last backup to this BackupVault
  public var lastBackupState: BackupConfigInfo.LastBackupState = BackupConfigInfo.LastBackupState()

  /// Output only. If the last backup were successful, this field has the
  /// consistency date.
  public var lastSuccessfulBackupConsistencyTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. If the last backup failed, this field has the error message.
  public var lastBackupError: GoogleRpc.Status? = nil

  /// Configuration Info has the resource format-specific configuration.
  public var backupConfig: OneOf_BackupConfig? = nil

  /// Initialize a new instance of `BackupConfigInfo`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = BackupConfigInfo().with { $0.lastBackupState = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case lastBackupState = "lastBackupState"
    case lastSuccessfulBackupConsistencyTime = "lastSuccessfulBackupConsistencyTime"
    case lastBackupError = "lastBackupError"
    case gcpBackupConfig = "gcpBackupConfig"
    case backupApplianceBackupConfig = "backupApplianceBackupConfig"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.lastBackupState = try container.decode(
      BackupConfigInfo.LastBackupState.self, forKey: .lastBackupState)
    self.lastSuccessfulBackupConsistencyTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .lastSuccessfulBackupConsistencyTime)
    self.lastBackupError = try container.decodeIfPresent(
      GoogleRpc.Status.self, forKey: .lastBackupError)

    var backupConfig: OneOf_BackupConfig? = nil
    let backupConfigCheckAndSet = {
      if backupConfig != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'backupConfig'"))
      }
      backupConfig = $0
    }
    if let gcpBackupConfig = try container.decodeIfPresent(
      GcpBackupConfig?.self, forKey: .gcpBackupConfig)
    {
      try backupConfigCheckAndSet(.gcpBackupConfig(gcpBackupConfig))
    }
    if let backupApplianceBackupConfig = try container.decodeIfPresent(
      BackupApplianceBackupConfig?.self, forKey: .backupApplianceBackupConfig)
    {
      try backupConfigCheckAndSet(.backupApplianceBackupConfig(backupApplianceBackupConfig))
    }
    self.backupConfig = backupConfig
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.lastBackupState, forKey: .lastBackupState)
    try container.encode(
      self.lastSuccessfulBackupConsistencyTime, forKey: .lastSuccessfulBackupConsistencyTime)
    try container.encode(self.lastBackupError, forKey: .lastBackupError)

    if let choice = self.backupConfig {
      switch choice {
      case .gcpBackupConfig(let value):
        try container.encode(value, forKey: .gcpBackupConfig)
      case .backupApplianceBackupConfig(let value):
        try container.encode(value, forKey: .backupApplianceBackupConfig)
      }
    }
  }

  /// LastBackupstate tracks whether the last backup was not yet started,
  /// successful, failed, or could not be run because of the lack of permissions.
  public enum LastBackupState: Codable, Equatable, Sendable {
    /// Status not set.
    case unspecified
    /// The first backup has not yet completed
    case firstBackupPending
    /// The most recent backup was successful
    case succeeded
    /// The most recent backup failed
    case failed
    /// The most recent backup could not be run/failed because of the lack of
    /// permissions
    case permissionDenied
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .firstBackupPending: return 1
      case .succeeded: return 2
      case .failed: return 3
      case .permissionDenied: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "LAST_BACKUP_STATE_UNSPECIFIED"
      case .firstBackupPending: return "FIRST_BACKUP_PENDING"
      case .succeeded: return "SUCCEEDED"
      case .failed: return "FAILED"
      case .permissionDenied: return "PERMISSION_DENIED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:LastBackupState/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "LAST_BACKUP_STATE_UNSPECIFIED": self = .unspecified
      case "FIRST_BACKUP_PENDING": self = .firstBackupPending
      case "SUCCEEDED": self = .succeeded
      case "FAILED": self = .failed
      case "PERMISSION_DENIED": self = .permissionDenied
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:LastBackupState/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .firstBackupPending
      case 2: self = .succeeded
      case 3: self = .failed
      case 4: self = .permissionDenied
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .firstBackupPending: return try container.encode(1)
      case .succeeded: return try container.encode(2)
      case .failed: return try container.encode(3)
      case .permissionDenied: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Configuration Info has the resource format-specific configuration.
  public enum OneOf_BackupConfig: Codable, Equatable, Sendable {
    /// Configuration for a Google Cloud resource.
    indirect case gcpBackupConfig(GcpBackupConfig?)
    /// Configuration for an application backed up by a Backup Appliance.
    indirect case backupApplianceBackupConfig(BackupApplianceBackupConfig?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.backupdr.v1.BackupConfigInfo"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
